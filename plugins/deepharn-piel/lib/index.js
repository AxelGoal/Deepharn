// Mitad de host. No hace nada por sí misma: existe para que el plugin sea una
// fila del árbol, y así el sistema de módulos de cliente sirva la mitad del
// navegador (./client) a la página.

export const name = 'deepharn-piel'

export function apply() {}
